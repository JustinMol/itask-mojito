import { SvgHoverableDirective } from './svg-hoverable.directive';

describe('SvgHoverableDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgHoverableDirective();
    expect(directive).toBeTruthy();
  });
});
