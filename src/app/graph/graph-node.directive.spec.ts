import { GraphNodeDirective } from './graph-node.directive';

describe('GraphNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphNodeDirective();
    expect(directive).toBeTruthy();
  });
});
