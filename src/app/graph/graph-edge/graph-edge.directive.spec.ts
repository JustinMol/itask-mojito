import { GraphEdgeDirective } from './graph-edge.directive';

describe('GraphEdgeDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphEdgeDirective();
    expect(directive).toBeTruthy();
  });
});
