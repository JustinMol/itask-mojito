import { MenuAccordionDirective } from './menu-accordion.directive';

describe('MenuAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
